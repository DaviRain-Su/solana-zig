//! Entrypoint placeholder — expanded in #102

pub fn processInstruction(program_id: @import("pubkey").Pubkey, accounts: []const u8, instruction_data: []const u8) u64 {
    _ = program_id;
    _ = accounts;
    _ = instruction_data;
    return 0;
}
