const solana_program = @import("solana_program");
const Pubkey = solana_program.pubkey.Pubkey;

export fn entrypoint() u64 {
    // hello-world minimal placeholder for #101 compile verification
    // will be replaced by real processInstruction in #104
    return 0;
}
